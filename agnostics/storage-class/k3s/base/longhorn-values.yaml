# Minimal Longhorn configuration for k3s dev
persistence:
  defaultClass: false  # rwx-default will be the default instead
  defaultClassReplicaCount: 1

defaultSettings:
  defaultReplicaCount: 1

# Pre-upgrade checker disabled
preUpgradeChecker:
  jobEnabled: false