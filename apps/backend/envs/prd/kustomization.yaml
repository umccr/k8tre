apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: backend

resources:
  - ../../base


patches:
  - path: image-patch.yaml
    target:
      kind: Deployment
      name: backend