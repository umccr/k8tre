apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: xk8tre-tls
  # namespace: cr8tor
spec:
  secretName: xk8tre-tls
  privateKey:
    rotationPolicy: Always
  commonName: cr8tor.${DOMAIN}
  dnsNames:
    - cr8tor.${DOMAIN}
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer