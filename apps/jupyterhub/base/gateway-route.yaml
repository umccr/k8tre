apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jupyter-route
  namespace: jupyterhub
spec:
  parentRefs:
  - name: internal-gateway
    namespace: gateway
  hostnames:
    - "jupyter.${DOMAIN}"
  rules:
    - backendRefs:
        - name: jhub-auth-proxy
          namespace: jupyterhub
          port: 8080