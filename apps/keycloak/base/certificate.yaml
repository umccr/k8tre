apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: xk8tre-tls
  namespace: keycloak
spec:
  secretName: keycloak.k8tre.${ENVIRONMENT}.${DOMAIN}-tls # pragma: allowlist secret
  privateKey:
    rotationPolicy: Always
  commonName: keycloak.k8tre.${ENVIRONMENT}.${DOMAIN}
  dnsNames:
    - keycloak.k8tre.${ENVIRONMENT}.${DOMAIN}
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
